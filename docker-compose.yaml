services:
  frontend:
    build:
      context: Frontend
    ports:
      - "5000:4000"
  backend:
    build:
      context: Backend
    volumes:
      - db-data:/backend/data/*
    ports:
      - "3000:3000"

volumes:
  db-data:
    driver: local
